# Programming - An Incantation

```python
from IPython.display import Audio
from math import sin, pi

sr, f0, d = 48000, 220, 0.1875

ts = [t/sr for t in range(int(d * sr))]

waves = [[sin(2 * pi * 2**(k/12) * f0 * t) for t in ts] for k in range(24)]
notes = {"rest" : [0 for t in ts]}

for k in range(24):
    notes["aåbcçd∂efƒghAÅBCÇDÎEFÏGÌ"[k]] = waves[k]

def make_audio(song):
    amps = []

    for event in song:
        if event in notes:
            amps += notes[event]
        else:
            amps += notes["rest"]

    return Audio(amps, rate = sr)

make_audio(4 * (2 * (2*"Bgeg" + 2*"Bƒ∂ƒ") + 2*"Bgeg" + "CgegDgCg" + 2*"Bgdg" + 2*"Bƒ∂ƒ"))
```
